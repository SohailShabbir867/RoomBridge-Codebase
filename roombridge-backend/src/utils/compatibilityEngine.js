const calculateCompatibility = (userPrefs, listingPrefs) => {
  // Simple compatibility engine stub
  // In a real app, this would compare specific fields
  return Math.floor(Math.random() * 101);
};

module.exports = { calculateCompatibility };
