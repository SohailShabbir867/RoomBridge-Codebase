const sendEmail = async (options) => {
  // Basic stub for email sending
  console.log('Sending email to:', options.email);
  console.log('Subject:', options.subject);
  console.log('Message:', options.message);
};

module.exports = sendEmail;
